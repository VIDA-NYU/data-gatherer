apiVersion: batch/v1
kind: Job
metadata:
  name: sequential-finetune
  namespace: data-gatherer
spec:
  template:
    spec:
      containers:
      - name: sequential-finetune
        image: pietromarini/data-gatherer-nvidia:20260501
        imagePullPolicy: Always
        command:
        - /bin/sh
        - -c
        - |
          set -e

          echo "=== [1/4] flan-t5-base ===" && \
          python3 /app/scripts/Local_model_finetuning/train_seq2seq.py \
            --pmc_links    /app/scripts/exp_input/REV.txt \
            --ground_truth /app/scripts/Local_model_finetuning/ground_truth/gt_dataset_info_no_dspage_extraction_from_snippet.xlsx \
            --output_dir   /data/flan-t5-models \
            --epochs       5 \
            --lr           3e-4 \
            --train_batch  2 \
            --eval_batch   2 \
            --grad_accum   8

          echo "=== [2/4] facebook/bart-base ===" && \
          python3 /app/scripts/Local_model_finetuning/train_seq2seq.py \
            --model_name   facebook/bart-base \
            --pmc_links    /app/scripts/exp_input/REV.txt \
            --ground_truth /app/scripts/Local_model_finetuning/ground_truth/gt_dataset_info_no_dspage_extraction_from_snippet.xlsx \
            --output_dir   /data/bart-base-models \
            --epochs       5 \
            --lr           3e-4 \
            --train_batch  2 \
            --eval_batch   2 \
            --grad_accum   8

          echo "=== [3/4] Salesforce/codet5p-220m ===" && \
          python3 /app/scripts/Local_model_finetuning/train_seq2seq.py \
            --model_name   Salesforce/codet5p-220m \
            --pmc_links    /app/scripts/exp_input/REV.txt \
            --ground_truth /app/scripts/Local_model_finetuning/ground_truth/gt_dataset_info_no_dspage_extraction_from_snippet.xlsx \
            --output_dir   /data/codet5p-220m-models \
            --epochs       5 \
            --lr           3e-4 \
            --train_batch  2 \
            --eval_batch   2 \
            --grad_accum   8

          echo "=== [4/4] google/long-t5-local-base ===" && \
          python3 /app/scripts/Local_model_finetuning/train_seq2seq.py \
            --model_name   google/long-t5-local-base \
            --pmc_links    /app/scripts/exp_input/REV.txt \
            --ground_truth /app/scripts/Local_model_finetuning/ground_truth/gt_dataset_info_no_dspage_extraction_from_snippet.xlsx \
            --output_dir   /data/long-t5-local-base-models \
            --epochs       5 \
            --lr           3e-4 \
            --train_batch  2 \
            --eval_batch   2 \
            --grad_accum   8 \
            --max_input    1024

          echo "=== All fine-tuning jobs complete ==="
        resources:
          limits:
            nvidia.com/gpu: "1"
            memory: "32Gi"
            cpu: "8"
          requests:
            nvidia.com/gpu: "1"
            memory: "32Gi"
            cpu: "8"
        env:
        - name: EMBEDDINGS_DEVICE
          value: "cuda"
        envFrom:
        - secretRef:
            name: data-gatherer-env-secret
        volumeMounts:
        - name: data-pvc
          mountPath: /data
        - name: shm
          mountPath: /dev/shm
      volumes:
      - name: data-pvc
        persistentVolumeClaim:
          claimName: data-gatherer-pvc
      - name: shm
        emptyDir:
          medium: Memory
          sizeLimit: 8Gi
      restartPolicy: Never
